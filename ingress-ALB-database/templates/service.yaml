apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingress-ALB-database.fullname" . }}
  labels:
    {{- include "ingress-ALB-database.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingress-ALB-database.selectorLabels" . | nindent 4 }}
